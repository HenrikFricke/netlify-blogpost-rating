{
  "scripts": {
    "build": "yarn build:likes && yarn build:functions",
    "build:likes": "node scripts/build.js",
    "build:functions": "netlify-lambda build functions"
  },
  "devDependencies": {
    "dotenv": "^5.0.1",
    "encoding": "^0.1.12",
    "netlify-lambda": "^0.4.0",
    "node-fetch": "^2.1.2"
  }
}
